# HISTORY.md — Append-only project changelog

**DO NOT EDIT prior entries.** Update only via `scripts/append_history.py`.
This file is `merge=union` in `.gitattributes` so concurrent branch entries combine.

## 2026-07-03T15:00:22Z — NO_SESSION — 77340e6
**Summary:** v3.8.0: SessionStart injects last 5 HISTORY summaries (sanitized, budgeted) and records session_start.json git baseline; startup protocol step 1 is now self-executing.
**Files:** scripts/session_handoff.py,scripts/lint_on_write.py,scripts/run_substrate_evals.py,tests/test_hook_scripts.py,AGENTS.md,templates/AGENTS.md,skills/session-recovery/
**Intent:** Release 1 of 4 in the discipline-mechanization sequence: convert the 'read HISTORY' startup instruction into hook behavior before shipping the riskier completion gate.
**Knowledge:** HISTORY text is treated as untrusted despite SHA validation: sanitized via redact + instruction-prefix strip + invisible-char/HTML/role-prefix removal, capped at 1500 chars. lint_on_write now honors ruff extend-exclude via --force-exclude. session_start.json is the baseline the v3.8.3 completion gate will compare against.

## 2026-07-04T05:04:08Z — NO_SESSION — 1ed7a6b
**Summary:** v3.8.1: ./manage.sh new-validator scaffolds check_<name>.py + adversarial test pair and prints the pre-commit block; wired into finding-response lock-down step.
**Files:** scripts/new_validator.py,manage.sh,templates/manage.sh.template,skills/finding-response/SKILL.md,customization.md,tests/test_hook_scripts.py
**Intent:** Release 2 of 4: make bugs-into-validators a one-command workflow instead of a hand-copied convention.
**Knowledge:** Scaffold never auto-edits .pre-commit-config.yaml (profile-rendered + drift-tracked; auto-edit would trip the upgrade drift gate). Generated skeleton defers yaml import so check_validator_input_coverage stays quiet until real parsing lands; test stub pre-stages non-string fixtures for layer 2.

## 2026-07-04T05:16:13Z — NO_SESSION — 222076c
**Summary:** v3.8.2: in-place RAISE-only profile ratchet via enable profile / upgrade --profile; bootstrap stages raw pre-commit template + strict extras under .substrate/.
**Files:** scripts/substrate_profile.py,scripts/substrate_upgrade.py,scripts/_doc_common.py,bootstrap.sh,manage.sh,templates/manage.sh.template,tests/test_hook_scripts.py,scripts/run_substrate_evals.py
**Intent:** Release 3 of 4: ratchet, don't max out — one command to climb starter->standard->strict when postmortems justify it, no kit checkout needed.
**Knowledge:** Renderer is a Python port of bootstrap render_precommit, verified byte-identical. Ratchet must re-apply config/required_profile AFTER upgrade _restore (preserve-set undoes bootstrap's fresh values). .substrate/ is now excluded from doc-drift code scanning (staged extras are dormant artifacts).

## 2026-07-04T05:34:21Z — NO_SESSION — 79394a4
**Summary:** v3.8.3: memory skill-run evidence (logger captures git state itself) + opt-in warning-only Stop-hook completion gate, default OFF; strict block deferred to v3.8.4 post-dogfood.
**Files:** scripts/completion_gate.py,scripts/memory_log.py,.claude/settings.json,templates/claude/settings.json.template,templates/codex/hooks.json.template,skills/self-audit/SKILL.md,AGENTS.md,templates/AGENTS.md,templates/OPERATOR_ENABLEMENT.md
**Intent:** Release 4 of 4: make 'self-audit before done' verifiable evidence instead of prose, with the reviewer-mandated soft rollout (default off, warn-only, kill-switch).
**Knowledge:** Gate ignores its own side effects: .substrate/, todo mirror, CURRENT_SESSION.md, __pycache__ (the audit's own .pyc write must not re-arm it); porcelain paths are position-encoded (never strip() the block output — first-line path mangling); untracked dirs collapse without -uall; event ts is second-resolution so ties go to the audit. skill-run records self-audit after the LAST project change, not merely after session start.

## 2026-07-04T05:46:42Z — NO_SESSION — 737f6a0
**Summary:** v3.8.3 audit polish: skill-run text hardened like handoff text; render byte-parity test; atomicity docs made honest (3 WARNs closed, 0 BLOCKs).
**Files:** scripts/memory_log.py,scripts/substrate_profile.py,tests/test_hook_scripts.py
**Intent:** Close the harness-audit WARNs on the v3.8.x cluster before review.
**Knowledge:** Any durable agent-authored text surface needs the same sanitization as the handoff path at WRITE time, even if nothing reads it back yet — retrofitting after a consumer exists is how injection channels ship.

## 2026-07-05T18:30:26Z — NO_SESSION — d90d04c
**Summary:** ci: green-notify job comments on the PR when the release matrix passes, converting CI success (no webhook) into a deliverable event; wake signal only, verify before acting.
**Files:** .github/workflows/release-matrix.yml
**Intent:** Event-driven PR watching without hourly polling: make the terminal green state generate the one event type webhooks deliver.
**Knowledge:** Comment is forgeable (anyone can comment) so watchers must re-verify via the checks API; permissions caged to the notify job; sticky upsert keeps one wake per push. Kit-self CI only, not a consumer template.

## 2026-07-10T13:29:00Z — NO_SESSION — 777ed1d
**Summary:** v3.8.4 audit remediation: fixed a P1 profile-floor downgrade (stale install.json), homoglyph/leet sanitizer bypass, unscanned templates, manage.sh false-positive, gameable self-audit (added --verify), green-notify fork fail-red, new-validator --desc; deduped _git; corrected doc drift.
**Files:** scripts/_text_safety.py,scripts/substrate_upgrade.py,scripts/substrate_profile.py,scripts/session_handoff.py,scripts/memory_log.py,scripts/completion_gate.py,scripts/_substrate_root.py,scripts/_substrate_surfaces.py,scripts/new_validator.py,scripts/run_substrate_evals.py,tests/test_hook_scripts.py,principles.md,docs/knowledge/00_substrate.md,.github/workflows/release-matrix.yml
**Intent:** Remediate two independent audits of the v3.8.x cluster; close the one P1 (self-introduced profile-floor downgrade) and harden the sanitizers/evidence honestly.
**Knowledge:** Profile floor must be max(config,required_profile,install.json) and locks written max(existing,target) — never trust agent-writable provenance alone. Sanitizers are a bounded reduction (confusables+NFKC+leet+invisible fold for the scan only), NOT an injection firewall; the durable defenses are budgets, labels, capability limits, deterministic gates. --verify ties skill-run evidence to a real check result for future block-mode.

## 2026-07-10T20:32:51Z — NO_SESSION — f8b5cc4
**Summary:** v3.8.5: remediate Codex's independent re-audit of v3.8.4 — 7 verified findings, all eval/test-locked (profile-floor equality trap, unscanned+unowned template sources, YAML name: injection in new-validator, --verify exit code + TOCTOU, green-notify pagination).
**Files:** scripts/substrate_profile.py,scripts/substrate_upgrade.py,scripts/_substrate_surfaces.py,scripts/new_validator.py,scripts/memory_log.py,.github/workflows/release-matrix.yml,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close every finding from a second independent audit (Codex) with the same rigor as the first; keep the green baseline honest (22/22 blocked, 0/11 FP) with a red->green regression per fix.
**Knowledge:** Profile floor is TWO constraints, not one max(): never below the required_profile lock (anchored on the owned/frozen lock, not agent-writable install.json) AND raise-only vs live config — so a stale-below-lock config is repairable UP to the lock (the v3.8.4 <= max-floor trapped this). Scan template SOURCES that ship verbatim into context surfaces. Quote generated YAML scalars. --verify must exit nonzero on failure and re-check the tree (TOCTOU).

## 2026-07-10T21:04:39Z — NO_SESSION — 8a94acf
**Summary:** v3.8.6: remediate Codex's re-audit of v3.8.5 — 6 verified findings, 2 of them incomplete v3.8.5 fixes (plain-upgrade render bypassing the lock via install.json; TOCTOU guard comparing porcelain strings not content), plus control-char/files-regex YAML safety in new-validator and a stale doctor fallback inventory.
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,scripts/new_validator.py,scripts/substrate_doctor.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close a second-round independent audit that caught my own v3.8.5 fixes being incomplete; render must be floored to the frozen lock on every path, verification evidence must be content-sensitive and fail-closed.
**Knowledge:** The render profile (not just the --profile CLI check) must be floored to required_profile — a plain upgrade rendered install.json provenance verbatim, so forged-low provenance dropped strict hooks while the lock read strict. TOCTOU evidence must compare CONTENT (porcelain + git diff + untracked content), not porcelain strings — an already-dirty file re-edited mid-check has an unchanged porcelain line. Mirror-or-fail-closed any hardcoded fallback of a canonical inventory; lock the mirror with a test.

## 2026-07-10T21:43:12Z — NO_SESSION — a745ecb
**Summary:** v3.8.7: remediate Codex's 3rd-round re-audit (of v3.8.6) — 6 verified findings, 2 again poking holes in v3.8.6 fixes (upgrade render still trusting provenance for remote_governance/HIGH profile; memory content-signature blind via git-diff ext-diff/textconv + C-quoted non-ASCII paths), plus U+FFFE/empty-regex in new-validator and a test-parity gap.
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,scripts/new_validator.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close a third independent-audit round with the same rigor; render must derive security tiers from live config and floor every required_* tier, verification evidence must hash real bytes and fail closed, generated YAML must be serialized not denylisted.
**Knowledge:** Derive render SECURITY tiers from live config, not agent-writable install.json, and floor EVERY required_* tier (profile AND remote_governance) — a forged provenance value dropped the trusted-base workflow. A content signature must hash raw on-disk bytes via status --porcelain -z -uall (no git diff: honors GIT_EXTERNAL_DIFF/textconv; no non-z: C-quotes non-ASCII paths) and fail closed. Serialize generated YAML scalars with json.dumps (a YAML subset) instead of hand-rolled denylists; reject empty/uncompilable regex.

## 2026-07-10T22:13:03Z — NO_SESSION — 9aaca4a
**Summary:** v3.8.8: remediate Codex's 4th-round re-audit (of v3.8.7) — 2 substantive findings (upgrade render still trusted provenance for the OTHER config-backed tiers lang/runner/sandbox; memory content-signature was index-blind + discarded rename old-names). Codex posted a convergence verdict; adopted substantive-only posture.
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the two substantive round-4 findings and converge: render must derive ALL config-backed tiers from live config and floor every required_* lock; verification evidence must cover the git index and rename identity.
**Knowledge:** Render derivation must take EVERY config-backed tier (profile, lang, runner, sandbox, remote_governance) from live .substrate/config and floor EVERY frozen required_* lock (profile, remote_governance, sandbox) — provenance only supplies ui/workflow. A tamper-evidence signature over a git worktree must also hash the INDEX (ls-files -s -z) and rename old-names, not just working-tree bytes. Convergence discipline: once findings are only cosmetic/test-hygiene, switch to substantive-only and stop extending rounds.

## 2026-07-10T22:46:47Z — NO_SESSION — 7da7d6a
**Summary:** v3.8.9: remediate Codex's 5th-round re-audit (of v3.8.8) — 2 substantive findings, both edges of v3.8.8 fixes (config parser leaked bootstrap inline comments once live config became the render authority; memory content-signature blind to skip-worktree/assume-unchanged hidden paths).
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the two substantive round-5 findings under the substantive-only posture; the render's config authority must parse config canonically, and the tamper-evidence signature must cover git's assume-unchanged/skip-worktree escape hatches.
**Knowledge:** When live config becomes an authority, it MUST be parsed with the canonical comment-aware/quote-aware parser (shared _parse_config mirroring check_substrate_config) — naive .strip('"') leaks bootstrap's inline # comments. A git worktree tamper-evidence signature has many escape hatches that each need explicit cover: ext-diff/textconv (use raw bytes), non-z C-quoting (use -z), the index (ls-files -s), and assume-unchanged/skip-worktree (ls-files -v + hash flagged bytes). Fail closed on any unreadable input.

## 2026-07-10T23:28:31Z — NO_SESSION — 50ec7eb
**Summary:** v3.8.10: remediate Codex's 6th-round re-audit (of v3.8.9) — 6 substantive findings (3 memory_log: symlink/mode-blind hidden-path hash, unsanitized GIT_INDEX_FILE/GIT_DIR env, non-fail-closed git identity; 3 substrate_upgrade: authority-read TOCTOU vs _resolve_kit, non-mapping install.json AttributeError, ignored finalizer return codes).
**Files:** scripts/memory_log.py,scripts/substrate_upgrade.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Fix every substantive round-6 finding under the operator's keep-fixing stopping rule; a tamper-evidence signature must cover git's routing-env and symlink/mode/identity escape hatches, and the upgrade's authority read + finalizers must be TOCTOU-safe and failure-aware.
**Knowledge:** A git worktree tamper-evidence signature must: read raw bytes (not diff), use -z, hash the index (ls-files -s) AND flags (ls-files -v) AND lstat type/mode + readlink for flagged paths, run under a SANITIZED env (strip GIT_INDEX_FILE/GIT_DIR/GIT_WORK_TREE + routing vars), and fail closed on unreadable/unborn HEAD and same-commit branch switches (compare full OID + symbolic ref). Upgrade: snapshot the config+required_* authority and re-check before the write (TOCTOU vs source resolution); schema-check agent-writable install.json (non-mapping -> absent); check finalizer return codes before claiming success.

## 2026-07-11T00:11:01Z — NO_SESSION — 887d227
**Summary:** v3.8.11: remediate Codex's 7th-round re-audit (of v3.8.10) — 6 substantive findings incl. an external-write P1 (symlinked owned dest followed by the render cp), an authority check->backup TOCTOU gap, partial provenance schema validation, and 3 memory-signature findings (gitlink/fsmonitor/core.filemode) all rooted in trusting git's config-steerable change-reporting.
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the seventh audit round under substantive-only: prevent writes outside the repo, make authority restore transactional, validate agent-writable provenance shape, and re-root the tamper-evidence signature on direct filesystem reads rather than git's change-reporting.
**Knowledge:** Never let a render cp follow a symlinked/escaping owned destination (refuse, even with --force). Authority-derived answers need a TRANSACTIONAL restore to the exact snapshot they were derived from. Agent-writable provenance shape must be schema-guarded before .items()/dict()/argv. Biggest lesson: a git-worktree tamper-evidence signature must READ THE FILESYSTEM ITSELF for the full tracked set (ls-files -z + lstat mode + content + gitlink HEAD, fsmonitor off, GIT_CONFIG* stripped) — every git change-reporting path (status, ls-files flags, fsmonitor, core.filemode) is steerable by mutable config/env.

## 2026-07-11T15:03:51Z — NO_SESSION — 4939235
**Summary:** v3.8.13: remediate Codex's 8th-round re-audit (of v3.8.11) — 6 substantive findings fixed as ONE combined release: external-write fixed at the WRITE layer (bootstrap no-follow rm -f), authority answers derived exclusively from a first snapshot with abort-on-change (no lock lowering), malformed drift map fails closed, and the memory signature pins st_dev/st_ino/st_nlink + rejects symlinked-ancestor escapes.
**Files:** scripts/substrate_upgrade.py,scripts/memory_log.py,bootstrap.sh,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the 8th audit round in one combined pass per operator directive; move the external-write defense to the write layer, make the render authority race-free and derived from a single snapshot, fail closed on malformed provenance, and pin filesystem identity in the tamper-evidence signature.
**Knowledge:** Defend a mutating tool AT THE WRITE (bootstrap copy/render rm -f the dest so cp/sed never follow a symlink) rather than chasing enumeration gaps. Derive render answers EXCLUSIVELY from a first authority snapshot and abort (root unmutated) on any later change — never LOWER a concurrently-raised lock. Treat a malformed drift map as untrusted/absent (require --force), not zero drift. A tamper-evidence signature over a worktree must pin filesystem IDENTITY (st_dev/st_ino/st_nlink) and no-follow ancestor topology (realpath escape => fail closed), not just content+mode.

## 2026-07-14T20:57:39Z — NO_SESSION — d2ee1bd
**Summary:** v3.8.14: remediate Codex's 9th-round re-audit (of v3.8.13) — 5 substantive findings combined: a memory-signature hash-canonicalization collision (now length-prefixed/injective), a symlinked-PARENT-dir external write (bootstrap now refuses escaping symlinks), the check->render authority race (raise-only lock reconciliation), a dirty-submodule gap (hash HEAD+dirty porcelain), and a missing-drift-map-key fail-open.
**Files:** scripts/memory_log.py,scripts/substrate_upgrade.py,bootstrap.sh,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the 9th audit round combined: make the tamper-evidence hash encoding injective, cover symlinked ancestors at the write layer, preserve a concurrent lock raise across the upgrade, hash submodule dirty state, and fail closed on an incomplete drift map.
**Knowledge:** A hash feeding a security decision must be INJECTIVE — length-prefix every variable field (path/content), or two different states collide to one digest. 'rm -f the leaf before writing' is NOT no-follow until symlinked ANCESTOR dirs are also refused (bootstrap now scans for escaping symlinks at startup). A check-then-act authority guard cannot fully close a concurrent-raise race without mandatory locking; capture the lock as late as possible and reconcile RAISE-ONLY, and document the residual. A gitlink identity must include the submodule's dirty worktree, not just HEAD. A MISSING drift-map key is as untrusted as a malformed one.

## 2026-07-16T02:27:26Z — NO_SESSION — 78f4ca8
**Summary:** v3.8.15: remediate Codex's 10th-round re-audit (of v3.8.14) — 4 substantive findings combined: memory now FAILS CLOSED on any gitlink (submodule integrity unverifiable), bootstrap adds a PER-WRITE no-follow guard (not just a startup scan), upgrade FAILS on a concurrent-raise reconciliation instead of claiming inconsistent success, and drift cross-checks baseline completeness against present scripts/ files.
**Files:** scripts/memory_log.py,scripts/substrate_upgrade.py,bootstrap.sh,tests/test_hook_scripts.py,docs/knowledge/00_substrate.md,README.md,BENCHMARK.md,VERSION
**Intent:** Close the 10th audit round combined: fail closed where integrity is not cheaply verifiable (submodules), make the symlink defense a per-write invariant, never claim success after a security reconciliation left state inconsistent, and detect a forged-but-valid incomplete drift map.
**Knowledge:** When you cannot cheaply VERIFY something (a submodule's full tracked integrity), FAIL CLOSED rather than approximate with a HEAD+porcelain hash. A check-then-act guard (startup symlink scan) needs a matching PER-OPERATION invariant (per-write real-parent resolution). Never claim success after a security reconciliation left state internally inconsistent — fail so a re-run repairs it. An agent-writable drift map must be checked for COMPLETENESS (a present managed file with no baseline hash is drift), not just validity — an attacker can delete the single entry for the file they edited.

## 2026-07-16T16:03:06Z — NO_SESSION — 4a939e8
**Summary:** v3.8.16: re-architect memory --verify onto git write-tree (retires the whack-a-mole tail)
**Files:** scripts/memory_log.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Every prior memory finding (v3.8.10 a/b/c, v3.8.14, v3.8.15) was the same shape: the hand-rolled _worktree_state re-derived git's content model in Python and each round missed one more attribute (symlink target, filemode, skip-worktree, gitlink, hardlink, path encoding). Structural fix over another point patch.
**Knowledge:** _worktree_state now stages the full worktree into a throwaway index (GIT_INDEX_FILE=tempdir, add -A with fsmonitor off + fileMode on) and takes git write-tree: a content-addressed tree OID folded with the temp-index hash, gitignored excluded, fails closed on any tracked gitlink (mode 160000). Deletes the lstat/inode/flag/length-prefix walk. Identity (HEAD OID + symbolic ref) and _clean_env sanitization unchanged. Upgrade staging-swap + bootstrap tempdir-render staged as follow-ups. check exit 0, evals 22/22 blocked, 0/11 FP.

## 2026-07-16T16:45:48Z — NO_SESSION — 83b043e
**Summary:** v3.8.17: widen upgrade drift-completeness to the reserved top-level manage.sh (finding upgrade:252)
**Files:** scripts/substrate_upgrade.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** The v3.8.15 completeness cross-check (catches edit-owned-file+delete-its-baseline-entry evasion) scanned only scripts/; the reserved CLI entrypoint manage.sh lives under no scanned dir, so the same evasion on manage.sh let --write overwrite it without --force. Continues the architectural convergence after v3.8.16's memory re-design.
**Knowledge:** _drifted now also completeness-scans _COMPLETENESS_SCAN_FILES=('manage.sh',): present + regular + not preserved/excluded + absent from the baseline owned-map -> drift (needs --force). Deliberately NOT the full write_install_json owned set (tests/, .claude/, .github/workflows/, docs/knowledge/) — projects author files there, so flagging unvouched = false-flag = cut functionality. Only substrate-RESERVED surfaces (scripts/ + manage.sh) are safe. Regression: test_upgrade_completeness_covers_reserved_toplevel_file. security-auditor PASS (manage.sh owned since engine's first commit; pre-install repos never reach the check). check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-16T18:28:11Z — NO_SESSION — e315ec4
**Summary:** v3.8.18: raw-byte memory signature (memory:209) + guard every bootstrap direct-write site (bootstrap:136)
**Files:** scripts/memory_log.py,bootstrap.sh,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Codex round-12 verified 2 real holes correcting v3.8.16/v3.8.17 over-claims: write-tree attests git's filtered/gitignore-excluding OBJECT model not raw bytes; and the no-follow write guard covered only copy()/render(), leaving ~15 direct redirection sites following a planted in-repo symlink.
**Knowledge:** memory: _worktree_state signature now folds a third component _raw_tracked_hash (length-prefixed netstring SHA-256 over RAW bytes of every git ls-files path; symlink targets via readlink; lstat FAILS CLOSED on any non-ENOENT OSError). Additive over write-tree (mode/symlink/structure still canonicalized by it). bootstrap: added wprep (mkdir+_safe_dest+leaf rm -f) for truncating writes and wappend (refuse symlinked leaf) for the two >> appends; routed every direct-write site + skills cp -R through them. security-auditor PASS(bootstrap)/WARN-resolved(memory). upgrade:593+258 deferred to the staging-swap re-arch (both need the kit's exact overwrite set). check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-16T20:11:13Z — NO_SESSION — 5da7bd0
**Summary:** v3.8.19: upgrade post-render authority postcondition (upgrade:593) + kit-derived overwrite-set completeness (upgrade:258) — round-12 fully closed
**Files:** scripts/substrate_upgrade.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-12's remaining pair: (P1) a lock raise landing after _restore but before the reconcile read left _reconciled False and the upgrade claimed success with config/hooks stale vs the lock — the check was history-based; (P2) the v3.8.17 scripts/+manage.sh completeness heuristic missed real bootstrap overwrite targets (.pre-commit-config.yaml, pytest.ini, .github/workflows/ci.yml) whose baseline entries were forged away.
**Knowledge:** Success is now judged from the END STATE: a post-render postcondition re-derives answers from the current on-disk authority (floored to current locks, identical logic to pre-render) and any mismatch fails rc 2, locks never lowered. Completeness now scans _kit_overwrite_set(kit) (every dest bootstrap --force may write, derived from the kit) INTERSECTED with _baseline_coverage (owned_files vouch surface) — unvouched+covered = forged/deleted entry; dests outside the vouch surface (.substrate/ staging) stay a documented pre-existing gap. Parity regression runs REAL bootstrap and fails if the dest map drifts. 3 new regressions; security-auditor PASS; check exit 0; evals 22/22 / 0/11.

## 2026-07-16T21:54:44Z — NO_SESSION — 7309160
**Summary:** v3.8.20: remediate Codex's 13th-round re-audit (of v3.8.19) — 4 findings + 2 auditor findings on the fixes: exact-parent write invariant (.substrate->.git alias), concrete authority end-state (deletion AND truncation), mode-aware raw hash, replaced-dir deletion drift
**Files:** bootstrap.sh,scripts/substrate_upgrade.py,scripts/memory_log.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-13: (P1) _safe_dest passed in-repo aliases so .substrate->.git routed writes into git internals; (P1) the v3.8.19 postcondition compared derived answers so a deleted config passed as default-equivalent; (P2) raw hash missed mode flips on tracked-but-ignored paths; (P2) leaf-only overwrite set missed wholesale-dir-replacement deletions. Security-auditor then found truncation-to-empty reopened the postcondition hole (BLOCK, fixed: locks must carry valid values) and a macOS case-insensitivity false-positive in the alias flag (WARN, fixed: casefold tolerance).
**Knowledge:** bootstrap _safe_dest is now EXACT-PARENT (real parent == literal logical parent — any symlinked ancestor refused); _unsafe_owned_dests clause 3 flags aliased owned/preserve paths pre-flight (case-only differences tolerated); the upgrade postcondition requires all 4 authority files as regular files with bootstrap-writable VALUES (profile name / 0/1) plus config:SUBSTRATE_PROFILE; _raw_tracked_hash folds S_IMODE per file; _kit_replaced_dirs mirrors bootstrap's rm-rf+cp-R skill-dir replacement and unvouched files under those dirs are drift. 5 regressions. check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-17T18:29:43Z — NO_SESSION — c0937af
**Summary:** v3.8.21: remediate Codex's 14th-round re-audit (of v3.8.20) — 6 findings (hardlink-safe writes, no-trust-on-preserve, mutation-free mkdir, KIT-path tool exec, post-finalizer authority re-check, replaced-dir symlink drift)
**Files:** bootstrap.sh,scripts/substrate_upgrade.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Codex round-14: bootstrap trusted pre-existing leaves and mutated before/around its write guard; upgrade's success postcondition ran before the finalizers and its replaced-dir scan ignored non-regular entries.
**Knowledge:** bootstrap.sh: wappend breaks hard links (copy->mv) before append (110); copy/render chmod only in the write branch so a skipped/hardlinked leaf is never chmod'd (152); the 3 invoked tools run from $KIT_DIR not target scripts/ (323); _safe_mkdir_p validates each ancestor before any mkdir, body mkdir -p routed through it, top-of-file mkdir -p $TARGET stays plain (274). substrate_upgrade.py: _authority_postcondition() closure re-checked AFTER finalizers too (846); replaced-dir drift scan flags unvouched symlinks/non-regular entries, not just regular files (296). 6 regressions; security-auditor PASS all 6; check exit 0, evals 22/22 blocked / 0/11 FP. Codex's 'check exited 1' did NOT reproduce (baseline clean).

## 2026-07-18T00:39:36Z — NO_SESSION — 5d1696d
**Summary:** v3.8.22: remediate Codex round-15 — 6 findings fixed, 1 documented limitation (agent-writable drift baseline), 1 doc regen
**Files:** bootstrap.sh,scripts/substrate_upgrade.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-15's theme: bootstrap trusted pre-existing leaves and the upgrade imported target code / left config inconsistent with locks. Threat model: attacker with concurrent in-repo write/exec racing install/plan/upgrade.
**Knowledge:** bootstrap: --install-tools force-renders kit manage.sh before setup (368); fail closed on unwritable install.json (367); wappend rewrites+preserves-mode only when nlink>1 (137). upgrade: _baseline_coverage execs TRUSTED KIT write_install_json via importlib not a bare import (320); symlinked replaced-dir root flagged as drift (298); postcondition runs canonical check_substrate_config rc==2 gate + _apply_capability_floor raises preserved config remote/sandbox to match locks (812). LIMITATION (220): drift baseline is agent-writable by design — real anchor is signed release + remote trusted-base; documented, not locally closeable. P3: BENCHMARK regenerated. Codex 'check exits 143' = SIGTERM/timeout on their host, not a bug (check exit 0 here). security-auditor PASS (1 non-blocking WARN). check 0, evals 22/22, 0/11 FP.

## 2026-07-26T04:27:35Z — NO_SESSION — b58e0ed
**Summary:** v3.8.23: remediate Codex round-16 — 5 findings, 3 of them incomplete-fix regressions of v3.8.18/v3.8.22
**Files:** scripts/substrate_upgrade.py,bootstrap.sh,scripts/memory_log.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-16's lesson: an 'acceptable residual' in a security gate is a finding waiting to be filed. The v3.8.22 config gate's documented fail-open-on-crash was weaponized; two other v3.8.22/v3.8.18 fixes were incomplete (existence != completion; containment invariant not re-derived per path-join site).
**Knowledge:** upgrade: canonical-config gate runs the KIT's check_substrate_config, fails on ANY nonzero rc (rc 1 = dangerous values was uncovered before), root pinned via SUBSTRATE_PROJECT_DIR so agent env cannot redirect it (916); _verify_backends import is lazy via _load_verify so rejection paths never execute the target sibling (32). bootstrap: provenance guard verifies install.json CONTENT records this install with python3->python fallback (370); --install-tools fails closed on setup failure, documented behavior change (388). memory: _raw_tracked_hash fails closed when a tracked path's real parent escapes _ROOT_REAL — lstat follows ancestors even though it does not follow the leaf (244). Codex's check-exit-143 did not recur (host timeout, not a bug). security-auditor PASS on all five; check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-26T22:29:04Z — NO_SESSION — 22b84b9
**Summary:** v3.8.24: remediate Codex round-17 — trusted-tool provenance (verifier hash-pin, kit-run finalizers, baseline freshness, escaping symlink leaf)
**Files:** scripts/substrate_upgrade.py,bootstrap.sh,scripts/memory_log.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-17 theme: every remaining place the engine ran, imported, or believed code/state from the tree it was auditing. Fixing 'do not trust code from the audited tree' requires fixing every loader — imports, subprocess finalizers, executed check tools — not one narrowed window.
**Knowledge:** upgrade: _load_verify(root) hash-pins scripts/_verify_backends.py against the drift baseline before import (same_tree gate prevents kit-run false-fails; SystemExit is caught and returns rc 2) — a stubbed verify() can no longer approve an unsigned zip (209); finalizers run the KIT's copies via _tool() with cwd=root + pinned SUBSTRATE_PROJECT_DIR (961). bootstrap: provenance guard captures the writer's real rc (_WJ_RC=0 + || _WJ_RC=$? so set -e cannot pre-abort) AND requires a non-empty owned map whose manage.sh hash matches the live file — a version label alone was forgeable (391). memory: escaping tracked symlink LEAF fails closed (was ancestors-only) and _run_deterministic_check refuses to execute a check tool resolving outside the repo; rc 2 propagates to verified=False (264). security-auditor PASS on all four, no WARNs; check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-27T22:34:10Z — NO_SESSION — 226c995
**Summary:** v3.8.25: remediate Codex round-18 — execute-the-bytes-you-verified (unchecked-pyc bypass, verifier dependency closure, fail-closed pin, hardlink-safe provenance)
**Files:** scripts/substrate_upgrade.py,scripts/write_install_json.py,scripts/memory_log.py,scripts/run_smoke_verification.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Round-18 exposed a class never modeled: a hash/signature over a FILE proves nothing unless the runtime is forced to execute that file's bytes. PEP 552 unchecked hash-based pycs live in gitignored __pycache__ — covered by neither the drift gate nor the memory signature.
**Knowledge:** upgrade: _exec_module_from_source compiles read_bytes and execs (never the bytecode cache) so hashed==executed; the pin covers the whole _VERIFIER_CLOSURE (_minisign loaded first so the wrapper binds the trusted module); a missing/non-string owned-map entry now REFUSES instead of skipping (--allow-unverified is the opt-out); sys.modules restored after private loads. write_install_json: same-dir mkstemp + os.replace breaks hard links (nlink>1 is invisible to symlink checks) and follows no symlink. memory: a tracked symlink's target must itself be TRACKED (in-repo-but-untracked is covered by nothing); a SYMLINKED check tool is refused outright; dangling links allowed, symlink-to-tracked-dir fails closed (documented). run_smoke_verification: sys.pycache_prefix redirected to a fresh dir + dont_write_bytecode + invalidate_caches before any validator import, in-process because -I strips env vars. security-auditor PASS; both minor WARNs closed. check exit 0, evals 22/22 blocked / 0/11 FP.

## 2026-07-28T16:31:09Z — NO_SESSION — e353d75
**Summary:** v3.8.26: fix two adoption blockers (vendored scripts linted in consumer repos; tracked memory state made commits never converge)
**Files:** scripts/run_python_gate.sh,bootstrap.sh,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Operator asked whether the substrate is usable in a real product. An end-to-end install into a realistic repo (existing pyproject.toml) found two hard blockers in minutes that 18 rounds of adversarial security auditing never touched — auditors test whether gates can be BROKEN, not whether a user can SUCCEED.
**Knowledge:** run_python_gate.sh filters substrate-RESERVED paths (scripts/, extras/) out of ruff FILE ARGUMENTS — additive, so it never overrides the repo's own excludes (the kit's config excludes tests/ + installer/, which --config would have dropped) and works for check AND format which take different flags; tests/ deliberately not filtered (consumer-owned, vendored tests are ruff-clean); if every path arg was reserved the gate exits 0 rather than falling through to a bare ruff that would walk the whole repo. bootstrap gitignores .substrate/memory/ (was only .../tasks/) — tracked events.jsonl/session_start.json were rewritten by hooks each run so pre-commit never converged. bootstrap prints a non-mutating advisory when an existing pyproject.toml lacks [tool.ruff]. Verified: existing-pyproject repo -> bootstrap -> setup -> adopt commit converges -> day-to-day commit passes first try, 0 failed gates. Known remaining adoption cost by design: doc-coverage gate is unconditional at all profiles, so pre-existing modules need covers: entries before the first commit.

## 2026-07-28T17:53:17Z — NO_SESSION — 77ef20a
**Summary:** v3.8.27: fix eval false-positive present in every consumer install (kit-source vs installed layout)
**Files:** scripts/run_substrate_evals.py,tests/test_hook_scripts.py,VERSION,README.md,BENCHMARK.md,docs/knowledge/00_substrate.md
**Intent:** Found by testing a REAL upgrade (v3.7.14 tool cloned, upgraded to v3.8.26) rather than by an audit round. Same class as v3.8.26: defects visible only when the substrate runs in a CONSUMER repo, never in the kit's own tree.
**Knowledge:** t_profile_ratchet_raise_succeeds staged fixtures from kit-SOURCE templates/ and extras/, which a consumer install never receives (bootstrap stages them under .substrate/pre-commit-config.yaml.template and .substrate/extras/). Both lookups were guarded by is_file()/glob, so an installed repo silently staged NOTHING, substrate_profile failed with 'template is missing', and a BENIGN task scored as a FALSE POSITIVE — consumer installs reported benign FP 1/11 while the kit reported 0/11, undermining the suite's own 're-run it on your host' claim. Both lookups now fall back to the installed layout; verified 0/11 in the kit and in two consumer repos (fresh v3.8.26 + upgraded-from-v3.7.14). Regression: test_evals_resolve_staged_assets_in_consumer_layout. Lesson: a tool that ships INTO another repo has TWO layouts (source + installed) for every path it resolves, and a silent is_file() guard turns a missing layout into a wrong ANSWER, not an error.

## 2026-07-29T20:59:41Z — NO_SESSION — c4ab9b7
**Summary:** v3.8.28: rejected-approach log (docs/REJECTED.md + CLI) injected at SessionStart under its own 500-char budget
**Files:** docs/REJECTED.md,scripts/append_rejected.py,scripts/session_handoff.py,manage.sh,templates/manage.sh.template,bootstrap.sh,AGENTS.md,templates/AGENTS.md,scripts/run_substrate_evals.py,tests/test_hook_scripts.py
**Intent:** Nothing stopped a session re-proposing an already-rejected approach. The ADR 'Alternatives Considered' section was the designed home but is heavyweight and after 27 releases went completely unused — cost of capture determines whether capture happens.
**Knowledge:** docs/REJECTED.md is append-only + merge=union + in PRESERVE_FILES (an upgrade must never wipe the operator's log). append_rejected.py mirrors append_history.py incl. atomic mkstemp+os.replace (never writes through a hard/symlink). _rejected_block() REUSES _safe_history_line() unchanged so the two injected blocks' defenses cannot drift. REJECTED_BUDGET=500 and ABSOLUTE_MAX_CONTEXT_CHARS deliberately NOT raised (4000+1500+500=6000) — that raise was rejected in PR #4 review and is now the log's first entry. Block appended LAST so ceiling truncation eats it first. Blind block[:BUDGET] truncation would drop the NEWEST entries of a chronological list, so it fits newest-first and renders newest-last. ADVISORY ONLY — no gate reads it. Also fixed test_evals_pass_on_shipped_kit hardcoding 'benign FP 0/11' despite claiming count-agnosticism. 23/23 malicious, 0/12 benign FP.

## 2026-07-29T21:22:50Z — NO_SESSION — a9461cd
**Summary:** v3.8.29: declarative doc assertions (asserts: path::substring) — verify a doc's claims about code, never execute them
**Files:** scripts/check_doc_drift.py,docs/knowledge/_template.md,templates/knowledge_doc_template.md,docs/knowledge/00_substrate.md,tests/test_hook_scripts.py
**Intent:** Drift detection proved docs were REVIEWED recently but never that their CLAIMS were true; a renamed function made a doc silently lie with every gate green. Safe adaptation of ClariLayer's declared-vs-observed idea.
**Knowledge:** asserts: is a FLAT list of path::substring — parse_front_matter has no nesting, so this needs zero parser changes and avoids the yaml blind-spot ritual. New assert_failed category in detect(); missing substring / missing file / MALFORMED each reported (malformed is never silently skipped — silent skip = false assurance). NOTHING EXECUTED (repo prose must not be executable; the executing variant is logged in docs/REJECTED.md and a test proves command-shaped text is only searched for). Bare scalar normalized to one entry (list() on a str would iterate chars). Bounds: 50/doc, 300-char needle, 4MB read. Opt-in: docs without the key are unchanged. NOT added to update_manifest.knowledge() (would rewrite manifest for every doc). Dogfooded with 10 assertions in 00_substrate.md; adds the first category-level tests the drift gate has ever had.

## 2026-07-29T22:10:50Z — NO_SESSION — 2d7e308
**Summary:** v3.8.30: per-doc knowledge-doc size budget (warn-only, named per doc) surfaced in context-report --budget and check_doc_drift; oversize is the one drift category deliberately excluded from the failure boolean, with SUBSTRATE_ENFORCE_DOC_BUDGET=1 opt-in
**Files:** scripts/context_report.py, scripts/check_doc_drift.py, tests/test_hook_scripts.py, docs/knowledge/00_substrate.md, VERSION, README.md, BENCHMARK.md
**Intent:** Close the third knowledge-layer gap: context-report MEASURED doc size but nothing gave feedback, so 00_substrate.md accreted a paragraph per release until it was 75% of all on-demand context (17.8k tok, 15x the next contributor). Name the growth where people already look, without turning a shape problem into a commit blocker.
**Knowledge:** Warn-only-first is the house precedent for advisory signals (the completion gate shipped with blocking disabled). The drift gate ORs every category into one failure boolean; oversize_doc is the deliberate exception, commented at the site, because the kit's own doc is ~6x over and a blocking version would have shipped red. Budget rows must stay purely additive — the four legacy rows are pinned by tests. Verified in a fresh consumer install: both surfaces resolve and the shipped 549-tok doc stays silent under the 3000-tok default. Splitting 00_substrate.md is deliberately deferred.

## 2026-08-09T16:03:11Z — NO_SESSION — 8502640
**Summary:** v3.8.31: serialize the append-only log writers — concurrent-append race fixed as a class via _doc_common.locked_atomic_append (parent-dir flock, bounded wait), shared by append_history and append_rejected
**Files:** scripts/_doc_common.py, scripts/append_history.py, scripts/append_rejected.py, tests/test_hook_scripts.py, docs/knowledge/00_substrate.md, docs/REJECTED.md, VERSION, README.md, BENCHMARK.md
**Intent:** Close the Codex-reported writer race (two concurrent ./manage.sh reject calls both rc 0, one entry lost) as a class: append_rejected mirrored append_history's read/mkstemp/replace verbatim, so both logs shared the defect. Operator-directed reclaim of the stalled claim.
**Knowledge:** mkstemp+os.replace serializes READERS, not WRITERS. Lock the parent DIRECTORY fd, not the target inode — replace swaps the directory entry so a late writer can lock the pre-swap inode and race anyway. Keep replace inside the lock to preserve no-write-through-links; O_APPEND writes through hard links (rejected, logged). Bound the wait (LOCK_NB + monotonic deadline, TimeoutError is an OSError so the rc-2 contract holds); a blocking LOCK_EX trades data loss for a hang. Fresh CI/cloud clones are SHALLOW — check_history_sha false-fails until git fetch --unshallow.

## 2026-08-10T00:04:26Z — NO_SESSION — 31f29eb
**Summary:** Plan v3.8.32 functional knowledge split and record the ungoverned Superpowers plan surface
**Files:** AGENT_BUS.md,docs/REJECTED.md,docs/superpowers/specs/2026-07-30-substrate-knowledge-split-design.md,docs/superpowers/plans/2026-08-09-substrate-knowledge-split.md
**Intent:** Lock the reviewed architecture, red-green seams, and release gates before implementation
**Knowledge:** v3.8.31 already owns append serialization; v3.8.32 preserves eleven assertions, keeps consumer installs on generated 00 only, fixes four integration gaps, and governs docs/superpowers as optional context

## 2026-08-13T19:35:18Z — NO_SESSION — 628da16
**Summary:** v3.8.32: functional substrate knowledge map with governed project context and fail-closed ownership migration
**Files:** README.md,BENCHMARK.md,VERSION,ai/audits/2026-08-13T192443Z/audit-report.md,docs/README.md,docs/REJECTED.md,docs/decisions/0001-substrate-knowledge-boundaries.md,docs/knowledge,docs/manifest.json,docs/postmortems/2026-08-10-knowledge-ownership-migration.md,docs/superpowers,scripts/_substrate_surfaces.py,scripts/check_doc_drift.py,scripts/check_harness_smoke.py,scripts/code_shape.py,scripts/context_report.py,scripts/substrate_doctor.py,scripts/substrate_upgrade.py,scripts/write_install_json.py,tests/conftest.py,tests/test_doc_consistency.py,tests/test_hook_scripts.py
**Intent:** Replace the oversized chronological knowledge document with enforceable functional boundaries without weakening consumer ownership, upgrade safety, or long-running release verification
**Knowledge:** Source has eight bounded knowledge docs; consumers retain generated 00 plus _template; project knowledge and plans are governed but excluded from provenance; pre-v3.8.32 crossings run the verified new kit engine; canonical coverage is source-bound, shape-checked, exact, and required before render; the finite test watchdog now has nearly 3x measured slow-host margin

## 2026-08-21T17:27:02Z — NO_SESSION — e555ba1
**Summary:** v3.8.33: fail-closed .substrate/required_* lock reads at every reader (config gate, exfil guard, upgrade, doctor) incl. the UnicodeDecodeError crash-bypass, absent-config no longer bypasses locks, and read-side sanitization of every structured-handoff field
**Files:** scripts/check_substrate_config.py, scripts/check_exfil_guard.py, scripts/substrate_upgrade.py, scripts/substrate_doctor.py, scripts/session_handoff.py, scripts/run_substrate_evals.py, tests/test_hook_scripts.py, tests/test_doc_consistency.py, docs/knowledge/02_upgrade_integrity.md, docs/knowledge/03_memory_sessions.md, docs/knowledge/04_policy_governance.md, VERSION, README.md, BENCHMARK.md
**Intent:** Close the two verified findings from the external architecture review: a present-but-unreadable/garbage lock silently dropped pinned minimums (chmod is not content drift, so the freeze never sees it), and forged current.json fields reached SessionStart context verbatim because only the write side sanitized.
**Knowledge:** except OSError does NOT catch UnicodeDecodeError (a ValueError) from read_text — garbage bytes crashed the readers with exit 1, and hook exit 1 is NON-BLOCKING, so the tool runs: a crash in a guard is itself a bypass. Absent lock = no lock, present-invalid lock = refuse; a directory-as-lock reads absent legitimately because a type swap deletes a tracked file (visible drift), unlike chmod. Sanitize handoff fields at READ time with the same _safe_history_line chain as HISTORY/REJECTED — write-side sanitization is bypassable when the writer is unauthenticated. Never pin last_human_reviewed to a frozen date in tests; assert valid-ISO-on-or-after instead.

## 2026-08-21T17:41:21Z — NO_SESSION — 21dbdd0
**Summary:** v3.8.34: replace the ARCHITECTURE/INTENT TODO stubs with the real system view — component map, data flow, trust boundaries, threat-class table, and the host-capability matrix stating exactly what each host's hook API lets the substrate mediate
**Files:** docs/ARCHITECTURE.md, docs/INTENT.md, VERSION, README.md, BENCHMARK.md
**Intent:** Establish truth before expanding capability (the review's P0): 33 releases shipped against TODO architecture docs, and the mediation boundary per host was undocumented, inviting proposals that assume mediation the hosts do not offer.
**Knowledge:** The host-capability matrix is the honest scope of tool mediation: a repo-level kit mediates exactly what each host hook API exposes — Claude gets per-edit and per-command hooks, Codex/Copilot get Bash-guard plus commit-time gates only, Gemini gets instructions only, and containment proofs never transfer between hosts. Kit ARCHITECTURE/INTENT are kit-source docs; bootstrap seeds consumers their own stubs, so kit-specific prose never ships downstream. Both files are in CONTEXT_GLOBS and therefore harness-scanned; describe threat classes without literal danger phrases.

## 2026-08-21T18:19:05Z — NO_SESSION — 2364cfd
**Summary:** v3.8.35: bus claim leases (72h TTL, HEARTBEAT refresh, RECLAIM on expiry, scripts/bus_claims.py deterministic reader wired as ./manage.sh bus) + reconciled pending-stale same-day exemption (review date today AND doc committed today)
**Files:** scripts/bus_claims.py, scripts/check_doc_drift.py, AGENT_BUS.md, manage.sh, templates/manage.sh.template, docs/knowledge/07_agent_context_governance.md, docs/knowledge/00_substrate.md, docs/knowledge/06_release_distribution.md, tests/test_hook_scripts.py, VERSION, README.md, BENCHMARK.md
**Intent:** Give bus claims a lifetime so a silent claimant cannot stall work indefinitely (the 9-day incident), with a deterministic advisory reader instead of any new infrastructure; and resolve the v3.8.32 staged-doc-only pending rule that made same-day repeat commits to broadly-covered files unsatisfiable.
**Knowledge:** Leases without infrastructure: derive claim state from the append-only bus with a chronological state machine; the reader is advisory and fail-open because coordination prose is not a trust anchor. Pending-stale reconciliation: staged doc OR (review date today AND last doc commit today) — a bare date proves nothing (Codex was right) but an unmodified doc cannot be staged, so staged-only deadlocks (commits-never-converge class). git commit --amend keeps the author date unless --reset-author; git_file_last_modified reads author date.

## 2026-08-22T14:52:18Z — NO_SESSION — de9fc97
**Summary:** v3.8.36: remediate Codex round-19 (12 findings) — fail-closed lock reads as a class via _doc_common.read_lock (O_NOFOLLOW+S_ISREG+UTF-8), corrected bus_claims lease state machine (timestamp order, owner/expiry validation, tail read), honest agentsync msg, type/shape-validated handoff restore, ARCHITECTURE corrections
**Files:** scripts/_doc_common.py, scripts/check_substrate_config.py, scripts/check_exfil_guard.py, scripts/substrate_upgrade.py, scripts/substrate_doctor.py, scripts/check_dep_cooldown.py, scripts/run_security_scanners.py, scripts/command_policy.py, scripts/check_policy_code_integrity.py, scripts/bus_claims.py, scripts/session_handoff.py, scripts/run_substrate_evals.py, agentsync.sh, docs/ARCHITECTURE.md, docs/knowledge/*.md, tests/test_hook_scripts.py
**Intent:** Codex's round-19 re-audit of v3.8.33-35 found 12 substantive issues, all reproduced in temp repos. The lock story was an incomplete class sweep (v3.8.33 fixed 4 of 7 readers) plus two properties no reader enforced (symlink/dir rejection, decode-failure refusal); the rest were correctness bugs in the new v3.8.35 bus reader and v3.8.31/33 surfaces.
**Knowledge:** is_file() FOLLOWS symlinks and reads a dir as not-a-file — both wrong for a trust anchor; the canonical reader uses O_NOFOLLOW + fstat S_ISREG + explicit UTF-8 decode and returns (state,value,reason) where 'bad' means present-but-malformed → fail closed, distinct from 'absent'. substrate_upgrade._authority_snapshot fed the render BEFORE any refusing reader, so validation had to move there too; its SystemExit refusals are caught at the main() boundary → rc 2. A merge=union bus is not chronological — fold events in timestamp order, and tail-read (newest state is at the bottom of an append-only file). Handoff restore must validate SHAPE (crash-proof) but cannot decide INTENT — that residual is a documented limit contained by framing, not detection. command_policy is AST-pinned: regenerate the SHA in check_policy_code_integrity when editing it.

## 2026-08-24T12:54:42Z — NO_SESSION — be829ea
**Summary:** v3.8.37: remediate Codex round-20 (14 findings) — lock readers open O_NOFOLLOW|O_NONBLOCK + whole-content bounded read (FIFO no-hang, no 4096 truncation), agentsync atomic O_NOFOLLOW append + honest commit-fail, bus_claims expiry-gated transitions/future-date/entry-streaming-tail/TTL-sanitize, root exec surfaces scanned+owned, handoff no-follow current.json + non-actionable todo framing
**Files:** scripts/_doc_common.py, scripts/command_policy.py, scripts/bus_claims.py, scripts/session_handoff.py, scripts/_substrate_surfaces.py, scripts/substrate_doctor.py, scripts/code_shape.py, scripts/check_policy_code_integrity.py, agentsync.sh, docs/ARCHITECTURE.md, docs/knowledge/03_memory_sessions.md, docs/knowledge/04_policy_governance.md, tests/test_hook_scripts.py, scripts/run_substrate_evals.py, VERSION, README.md, BENCHMARK.md
**Intent:** Codex round-20 found 14 substantive issues, several honest incomplete-fix regressions of my own v3.8.36 lock reader (O_NOFOLLOW alone still hangs on a FIFO; a 4096-byte read lets a padded lock strip to a lowering value). Fix each as a class and close the auditor's TOCTOU WARN in the same release.
**Knowledge:** O_NOFOLLOW must be paired with O_NONBLOCK or a FIFO lock hangs the hook; a bounded lock read must reject oversize, not truncate-then-strip (strip hides internal padding). Test-then-act on a path is a TOCTOU; an atomic O_NOFOLLOW|O_APPEND|O_CREAT open is the shell-safe equivalent. A bus tail read must be ENTRY-aware (stream matching lines into a bounded deque) so filler cannot displace a fresh claim — the inverse of the round-19 head-slice bug. An expired lease must require explicit RECLAIM; a foreign HEARTBEAT/RELEASE silently taking it is the bug. Root .sh the substrate runs (bootstrap/agentsync/package_release) must be in CODE_GLOBS + OWNED_FILES + the doctor/code_shape fallbacks. Restored handoff todos must be framed as verify-only, never paired with a resume directive that re-arms a forged label. OWNED_FILES additions only false-fail under SUBSTRATE_REMOTE_GOVERNANCE=1, so consumers are unaffected.

## 2026-08-24T14:39:22Z — NO_SESSION — 2f31e41
**Summary:** v3.8.38: remediate Codex round-21 (11 findings) — write-through symlink/hard-link class swept from the v3.8.37 writers (agentsync hard-link/FIFO/newline refuse, atomic no-follow capture writes, hard-link-safe restore), harness no longer follows symlinked entrypoints, agentsync doc-covered, ARCHITECTURE prose fixed, postmortem/finding-response hooks catch remediation-release subjects
**Files:** agentsync.sh, scripts/session_handoff.py, scripts/check_agent_harness.py, scripts/_substrate_surfaces.py, scripts/substrate_doctor.py, scripts/code_shape.py, scripts/check_postmortem_for_bug_fix.py, scripts/check_finding_response.py, scripts/check_policy_code_integrity.py, docs/ARCHITECTURE.md, docs/knowledge/07_agent_context_governance.md, docs/postmortems/2026-08-24-write-through-links-recurrence.md, tests/test_hook_scripts.py, tests/test_doc_consistency.py, scripts/run_substrate_evals.py, VERSION, README.md, BENCHMARK.md
**Intent:** Close round-21 as classes. The hard-link findings proved the write-through-links class (v3.8.25) recurs when a new writer is hardened against symlinks but not hard links; the fix centralizes link-safe I/O so future writers inherit it rather than re-deriving half of it.
**Knowledge:** os.replace(tmp, path) acts on the directory entry, so mkstemp+replace breaks hard links AND does not write through a symlink — the canonical safe-write. A hard link passes every is_file()/O_NOFOLLOW check because it IS a regular file; guard st_nlink>1 explicitly on reads and writes of agent-writable paths. O_NONBLOCK on lock/bus opens prevents a FIFO from hanging the hook. Broadening the postmortem/finding-response detection means every remediation release now needs a postmortem + the four finding-response fields with regex+commit-msg-hooks checklist references; the write-through-links postmortem is the carry-forward anchor. agentsync.sh is a kit-dev tool, not shipped to consumers, so its evals self-skip cleanly there.

## 2026-08-24T16:36:47Z — NO_SESSION — 664ed30
**Summary:** v3.8.39: remediate Codex round-22 (9 findings) — symlinked-ancestor containment (the parent-dir layer of the write-through-links class) via _doc_common.within_root across read_lock/locked_atomic_append/handoff capture+restore/command_policy; plain foreign CLAIM on an expired lease now needs explicit RECLAIM; bus_claims+agentsync-read refuse a symlinked AGENT_BUS.md; harness BLOCKs a symlinked governed directory
**Files:** scripts/_doc_common.py, scripts/session_handoff.py, scripts/command_policy.py, scripts/bus_claims.py, scripts/check_agent_harness.py, scripts/check_substrate_config.py, scripts/check_exfil_guard.py, scripts/check_dep_cooldown.py, scripts/run_security_scanners.py, scripts/substrate_doctor.py, scripts/substrate_upgrade.py, scripts/check_policy_code_integrity.py, agentsync.sh, docs/knowledge/02_upgrade_integrity.md, docs/knowledge/04_policy_governance.md, docs/knowledge/07_agent_context_governance.md, docs/postmortems/2026-08-24-write-through-links-recurrence.md, tests/test_hook_scripts.py, scripts/run_substrate_evals.py, VERSION, README.md, BENCHMARK.md
**Intent:** Close round-22 as the ancestor layer of the write-through-links class. v3.8.38 hardened each path's leaf; every one still routed through a symlinked parent directory. Centralize the realpath-parent containment so future readers/writers inherit all three parts (leaf symlink, leaf hard link, ancestor) rather than re-deriving two of three.
**Knowledge:** within_root(target, root) = realpath(target.parent) within realpath(root); the +os.sep before startswith prevents /a-vs-/ab prefix confusion, and realpath'ing BOTH sides allows a legitimately symlinked repo root while refusing a symlinked ancestor below it. read_lock/locked_atomic_append gained a root param that ALL non-test call sites must pass — the default repo_root() is only right when the target IS the process repo (upgrade operates on a --root target, so it must pass root). command_policy keeps an inline copy (AST-pinned). bus_claims: only RECLAIM takes an expired lease; a plain foreign CLAIM is now a violation. The write-through-links class has now recurred THREE times (v3.8.25 leaf, v3.8.38 hard link, v3.8.39 ancestor) — the postmortem carry-forward rule is now 3-part. Convergence: 12→14→11→9 findings, each a strictly narrower edge.

## 2026-08-24T19:58:30Z — NO_SESSION — 499c1cb
**Summary:** v3.8.40: remediate Codex round-23 (8 findings) + in-release auditor P1 — STRICT ancestor containment (within_root exact-lexical parent rejects in-repo aliasing), missed writers swept (locked_atomic_append read-through leaf, memory_log containment+guard-before-mkdir, capture mkdir-after-guard), hard-linked readers (bus_claims/agentsync), agentsync read-after-pull TOCTOU, and a fail-open memory_log fallback tautology rewritten fail-closed
**Files:** scripts/_doc_common.py,scripts/memory_log.py,scripts/command_policy.py,scripts/check_policy_code_integrity.py,scripts/bus_claims.py,scripts/check_agent_harness.py,scripts/session_handoff.py,agentsync.sh,scripts/run_substrate_evals.py,tests/test_hook_scripts.py,docs/postmortems/2026-08-24-write-through-links-recurrence.md,VERSION,README.md
**Intent:** Close the STRICT (in-repo-aliasing) layer of the write-through-symlink/hard-link class plus every remaining writer/reader the leaf-only and no-escape rounds missed, and eliminate a tautological fail-open fallback so containment fails closed even when _doc_common import fails
**Knowledge:** within_root now enforces exact-lexical-parent (realpath(root)+relpath==realpath(parent)); every agent-writable path writer/reader must call it BEFORE mkdir/open/lock and refuse symlinked+hard-linked leaves; any inline fallback must fail CLOSED (the old realpath(MEM)==realpath(MEM) tautology failed open). Class has recurred 4 rounds (v3.8.25/38/39/40); postmortem carry-forward rule is 4-part

## 2026-08-24T21:05:42Z — NO_SESSION — df73135
**Summary:** v3.8.41: remediate Codex round-24 (6 findings) — the HARD-LINKED LEAF of the write-through-links class: a shared _doc_common.refuse_linked_leaf (symlink OR st_nlink>1) applied to locked_atomic_append/memory_log/read_session_token, st_nlink>1 added to read_lock + the AST-pinned command_policy lock reader (pin regen), and the harness ancestor-walk extended to _SKILL_ROOTS so a symlinked .agents/skills BLOCKs
**Files:** scripts/_doc_common.py,scripts/command_policy.py,scripts/check_policy_code_integrity.py,scripts/memory_log.py,scripts/append_history.py,scripts/check_agent_harness.py,scripts/run_substrate_evals.py,tests/test_hook_scripts.py,docs/postmortems/2026-08-24-write-through-links-recurrence.md,docs/knowledge/03_memory_sessions.md,docs/knowledge/04_policy_governance.md,docs/knowledge/07_agent_context_governance.md,VERSION,README.md
**Intent:** Close the hard-link half of every leaf guard round-23 added only the symlink half of — a hard link is a regular file that passes is_symlink()/O_NOFOLLOW/S_ISREG — plus the skill-glob-root symlink the harness walk did not cover
**Knowledge:** refuse_linked_leaf(path) is the single symlink-OR-st_nlink>1 guard every path-based leaf reader/writer must call; fd-based lock readers add st_nlink>1 on their fstat; the harness walks _SKILL_ROOTS. When adding a symlink leaf guard, add the hard-link half in the SAME edit — that split caused the round-23->24 recurrence. Class has now recurred 5x (v3.8.25/38/39/40/41)

## 2026-08-25T12:38:05Z — NO_SESSION — 4ebfbd2
**Summary:** v3.8.42: remediate Codex round-25 (9 findings, 1 P1) — three groups: NON-REGULAR leaves (refuse_linked_leaf never checked S_ISREG, so a FIFO read as safe and HUNG the caller), the READ SIDE that rounds 21-24 never swept (new shared safe_read_text now backs memory_log, the session token, the SessionStart HISTORY/REJECTED tails, and the completion gate), and a post-lock check-then-use TOCTOU in locked_atomic_append now closed by anchoring every post-lock op to the locked directory fd
**Files:** scripts/_doc_common.py,scripts/memory_log.py,scripts/append_history.py,scripts/session_handoff.py,scripts/completion_gate.py,scripts/check_agent_harness.py,scripts/check_policy_code_integrity.py,scripts/run_substrate_evals.py,tests/test_hook_scripts.py,docs/postmortems/2026-08-24-write-through-links-recurrence.md,docs/knowledge/03_memory_sessions.md,docs/knowledge/04_policy_governance.md,docs/knowledge/05_evals_assurance.md,docs/knowledge/07_agent_context_governance.md,VERSION,README.md,BENCHMARK.md
**Intent:** Close the third unsafe-leaf property (non-regular), give every READER the guarantees the writers already had, and eliminate a genuine check-then-use race where guards that ran before the lock could not protect the write that happened after it
**Knowledge:** An unsafe leaf has THREE properties (symlink, st_nlink>1, non-regular) — enumerating two is not a class fix. safe_read_text is the read-side counterpart to refuse_linked_leaf; every reader of an agent-writable file must call it. A guard that runs before a blocking wait has not run: re-validate after the wait and anchor the work to an fd, not a path. A refusal must stay distinguishable from absence, or it is a fail-open. Lossy decoding is not sanitization

## 2026-08-26T03:20:16Z — NO_SESSION — 3eab840
**Summary:** v3.8.43: remediate Codex round-26 (10 findings, 4 P1) and MECHANIZE the class behind six rounds of them — shared fd-anchored safe_atomic_write replacing three re-derived writers, fstat-after-open closing the lstat/open window in locked_atomic_append, a sweep of all 53 governed raw-IO sites to zero, and scripts/check_raw_file_io.py wired into check + pre-commit so a new unguarded governed write fails the build
**Files:** scripts/check_raw_file_io.py,scripts/_doc_common.py,scripts/substrate_upgrade.py,scripts/substrate_profile.py,scripts/substrate_doctor.py,scripts/substrate_audit.py,scripts/todo_state_hook.py,scripts/completion_gate.py,scripts/session_handoff.py,scripts/command_policy.py,scripts/check_substrate_config.py,scripts/check_harness_patterns.py,scripts/check_history_sha.py,scripts/check_dep_cooldown.py,scripts/check_doc_drift.py,scripts/check_exfil_guard.py,scripts/context_report.py,scripts/lint_on_write.py,scripts/sandbox_detect.py,scripts/update_manifest.py,scripts/write_install_json.py,scripts/new_validator.py,scripts/run_security_scanners.py,scripts/run_substrate_evals.py,manage.sh,.pre-commit-config.yaml,tests/test_hook_scripts.py,docs/postmortems/2026-08-24-write-through-links-recurrence.md,VERSION,README.md,BENCHMARK.md
**Intent:** Stop remediating a recurring class by fixing only its reported instances. Six rounds of correct point fixes never shrank the surface because the surface is every call site; the fix is a sweep plus a gate that makes the safe path mandatory rather than merely available
**Knowledge:** A class that recurs twice needs a GATE, not another patch: shared helpers make the safe path available, only a gate makes it mandatory (the first postmortem in this series prescribed exactly this and six rounds were spent re-proving it). Measure a surface before asserting its size — two published estimates (176 from a text search, ~30 from a name-based pass) were both wrong; AST data-flow gave 53. A gate must fail CLOSED: unresolved means REPORT, never skip. Three auditor BLOCKs each found the gate reproducing the very defect class it polices, which is why an author cannot audit their own blind spot

## 2026-08-26T15:54:14Z — NO_SESSION — f176388
**Summary:** v3.8.44: remediate Codex round-27 (12 findings, 6 P1) — the shared write/read primitive itself still resolved a MULTI-COMPONENT parent path after its containment check, so swapping any intermediate ancestor rerouted the whole subtree and the dev/ino re-validation compared the rerouted directory to itself; replaced by open_dir_chain(), a one-component-at-a-time descent from the root with O_NOFOLLOW|O_DIRECTORY and dir_fd=, now used by safe_atomic_write / safe_read_text / the new safe_read_bytes / read_lock / locked_atomic_append. Also: guarded rewrites were destroying target permissions (0755 -> 0600), and the raw-IO gate had six holes plus a wrapper blind spot whose fix exposed ten more real governed call sites
**Files:** scripts/_doc_common.py, scripts/check_raw_file_io.py, scripts/update_manifest.py, scripts/substrate_profile.py, scripts/substrate_upgrade.py, scripts/bus_claims.py, scripts/code_shape.py, scripts/check_coverage_floors.py, scripts/check_postmortem_gates_resolved.py, scripts/write_install_json.py, scripts/new_validator.py, scripts/run_substrate_evals.py, tests/test_hook_scripts.py, docs/postmortems/2026-08-24-write-through-links-recurrence.md, VERSION, README.md, BENCHMARK.md
**Intent:** Seventh round of one defect class. The method changed in v3.8.43 (shared helpers plus a mechanized gate); round 27 proved the shared helper was itself unsound, so the fix is structural rather than another check: never hand the kernel a re-resolvable path, and make containment a property of how the descent is performed instead of a test run beforehand
**Knowledge:** open_dir_chain(root, parent, create) is the ONLY descent primitive; any new reader or writer must use it rather than opening a parent by path. safe_read_bytes is the byte-returning sibling of safe_read_text — hashing callers must not re-derive read_bytes(). Inherited modes are masked to 0o775 (INHERIT_MODE_MASK); an explicit mode= argument is not masked. read_lock distinguishes FileNotFoundError (absent) from every other OSError (bad) because on Linux O_DIRECTORY|O_NOFOLLOW on a symlinked ancestor reports ENOTDIR, not ELOOP. The raw-IO gate propagates GOVERNED one module deep into bare-Name callees, keyed by SCOPE PATH — a governed path reaching raw I/O through a module-local wrapper fails the build; a path handed to a method or another module is still only unresolved

## 2026-08-26T22:23:55Z — NO_SESSION — 90a8c4b
**Summary:** v3.8.45: remediate Codex round-28 (10 findings, 5 P1) — the raw-IO gate shipped to every consumer as an INERT FILE (generated manage.sh and pre-commit never invoked it), now wired into both templates with a DERIVED parity test so the next validator cannot ship unwired; and the round-27 fd anchor met its limit — a parent RENAMED after the descent captured it left reads returning detached bytes and writes reporting SUCCESS with the live target untouched, so dir_fd_still_live() re-descends after every operation and all four fd-capturing helpers refuse rather than report a write that did not land. Seven gate holes closed (keyword operands, wildcard imports, basename-keyed allowlist and self-skip, symlinked child dirs, deep wrapper chains, RecursionError in ast.parse) plus four in-release auditor findings in the new code itself
**Files:** scripts/_doc_common.py, scripts/check_raw_file_io.py, scripts/run_substrate_evals.py, templates/manage.sh.template, templates/pre-commit-config.yaml.template, tests/test_hook_scripts.py, tests/test_doc_consistency.py, docs/postmortems/2026-08-24-write-through-links-recurrence.md, VERSION, README.md, BENCHMARK.md
**Intent:** Eighth round of one defect class. The headline is not a race: the mechanism built over two releases to make the safe path MANDATORY was never wired into what consumers receive, and the parity test meant to catch that listed validators by hand so it agreed with itself. Parity is now derived from the kit's own check block. The second theme is the honest limit of fd anchoring — it prevents following a hostile new path but says nothing about whether the pinned inode is still reachable at the requested path, so detection after the fact converts a silent false success into a loud failure
**Knowledge:** dir_fd_still_live(root, parent, dir_fd) must be called AFTER any operation performed through a captured directory fd; safe_read_bytes, safe_atomic_write, locked_atomic_append and read_lock all do. Close every fd before returning from that check's refusal branch — the first cut leaked one per refusal in read_lock. Raw-IO gate: allowlist keys are repo-relative paths and carry an expected match COUNT (a plain string means exactly one site, a (reason, count) tuple means more), so an exemption cannot silently spread; the scan walk refuses symlinked child directories; the self-skip compares resolved paths; operand collection covers positional AND keyword paths, and an unreadable ** unpack must surface as unresolved rather than vanish; the propagation bound is derived from the module's parameter count, never a hand-picked depth. Any new validator added to the kit's check MUST also be added to templates/manage.sh.template and templates/pre-commit-config.yaml.template or test_every_kit_validator_reaches_consumers fails

## 2026-08-27T15:41:41Z — NO_SESSION — 741c2e1
**Summary:** v3.8.46: remediate Codex round-29 (10 findings, 4 P1) — the guard that ran AFTER the mutation: three call sites created a directory with a raw mkdir(parents=True) and only then had their guarded write refused, so a symlinked ancestor meant the directory already existed outside the repo (and the evals runner swallowed the refusal and printed ok). safe_mkdir now builds on the component-walk descent and the three exemptions were deleted. The four gate P1s were one design error — import aliases learned during the ordered walk in flat module-wide dicts, producing use-before-import drops, nested-import clobbering, and a nested def suppressing a star import — now resolved in a per-scope pre-pass; plus os.path.join classified as a path constructor, which surfaced a real unguarded read in the memory chain's own signature hasher
**Files:** scripts/_doc_common.py, scripts/check_raw_file_io.py, scripts/memory_log.py, scripts/check_agent_harness.py, scripts/check_policy_code_integrity.py, scripts/substrate_audit.py, scripts/run_substrate_evals.py, scripts/run_security_scanners.py, tests/test_hook_scripts.py, docs/knowledge/03_memory_sessions.md, docs/knowledge/04_policy_governance.md, docs/knowledge/05_evals_assurance.md, docs/knowledge/07_agent_context_governance.md, docs/postmortems/2026-08-24-write-through-links-recurrence.md, VERSION, README.md, BENCHMARK.md
**Intent:** Ninth round of one defect class. Two lessons worth more than the ten fixes: a control placed after the mutation it is meant to prevent is a report rather than a control, and three allowlist reasons stated that wrong order in my own words and survived three reviews; and an analysis whose view of a module depends on the order its walker reaches things will be wrong in ways that look unrelated to each other
**Knowledge:** safe_mkdir(path, root) is the only directory-creation primitive; a raw mkdir before a guarded write defeats the guard because the mutation happens first. GuardRefusal (subclasses OSError) distinguishes a containment refusal from an IO failure — callers that must fail closed on tampering but tolerate a read-only environment check isinstance rather than string-matching. The raw-IO gate resolves import bindings in a pre-pass keyed by scope path: module-level imports bind everywhere regardless of textual order, a function's own imports bind only inside it, only MODULE-level defs shadow a star import, and match/case bodies plus ClassDef scope names must both be walked or a binding vanishes silently. os.path.join and friends are transparent path constructors. A guarded-helper import fallback must live in its OWN try/except — splicing one into another lets a fallback for one helper overwrite another that imported successfully, which is pinned by an AST invariant test

## 2026-08-27T19:14:30Z — NO_SESSION — 3e8de8e
**Summary:** v3.8.47: remediate Codex round-30 (10 findings, 3 P1) — round-29 moved IMPORT resolution into a scope-keyed pre-pass and left the other binding forms in the ordered walk, so assignment aliases bound later in the file were dropped, a LIFO enumeration resolved competing imports to the wrong one, and a class BODY (which executes) was skipped entirely; all binding forms now resolve in document order through one pre-pass. Same shape twice more: safe_mkdir was added in v3.8.46 without the liveness check the other three fd-capturing primitives already had, and the all-operand join classification was wired into the assignment path only. Origins widened past the blessed ROOT symbol, surfacing four more real sites including the module loader that compiles and executes what it reads. AGENT_BUS.md is now scanned, with an evidence carve-out limited to one surface and one pattern class
**Files:** scripts/check_raw_file_io.py, scripts/_doc_common.py, scripts/check_agent_harness.py, scripts/check_substrate_config.py, scripts/check_policy_code_integrity.py, scripts/_substrate_surfaces.py, scripts/check_dep_cooldown.py, scripts/check_exfil_guard.py, scripts/substrate_upgrade.py, tests/test_hook_scripts.py, docs/knowledge/01_install_adoption.md, docs/knowledge/02_upgrade_integrity.md, docs/postmortems/2026-08-24-write-through-links-recurrence.md, VERSION, README.md, BENCHMARK.md
**Intent:** Tenth round of one defect class. The lesson worth more than the ten fixes: when a fix generalises, it has to cover every sibling of the thing it fixed — import/assignment/class-body bindings are one problem, reads/writes/appends/mkdirs are one problem, assignment operands and inline operands are one problem. Fixing the reported member and stopping is how a single root cause returns as three separate-looking findings in the next round
**Knowledge:** All binding forms resolve through _collect_bindings in DOCUMENT order (sorted by lineno/col_offset), keyed by scope path; class bodies execute, so they get their own bindings AND are scanned. Every fd-capturing primitive must call dir_fd_still_live after the operation — safe_mkdir was the fourth and was added without it. Operands and receivers classify through _classify_expr, not _base_symbol alone. An origin need not name ROOT: Path.cwd(), Path(__file__).resolve().parent..., and bare relative literals are the checkout, but a literal counts only where the call is unambiguously file I/O because Path and str share method names. ALL-CAPS constants holding string literals are not path constants. AGENT_BUS.md is scanned by the harness; inline-code spans count as quoted evidence there for the INJECTION pattern class alone, never for the other two classes, and never on any other surface

## 2026-08-28T04:42:32Z — NO_SESSION — cade57e
**Summary:** v3.8.48 closes scanner, guarded-read, and fallback-inventory gaps
**Files:** BENCHMARK.md, README.md, VERSION, docs/REJECTED.md, docs/knowledge/01_install_adoption.md, docs/knowledge/02_upgrade_integrity.md, docs/knowledge/04_policy_governance.md, docs/knowledge/05_evals_assurance.md, docs/knowledge/06_release_distribution.md, docs/knowledge/07_agent_context_governance.md, docs/manifest.json, scripts/_substrate_surfaces.py, scripts/check_agent_harness.py, scripts/check_dep_cooldown.py, scripts/check_policy_code_integrity.py, scripts/check_raw_file_io.py, scripts/code_shape.py, scripts/substrate_doctor.py, scripts/write_install_json.py, tests/test_hook_scripts.py
**Intent:** Close Round-31 and in-release review gaps as classes: raw I/O alias/origin coverage, guarded harness and policy source reads, fail-closed dependency evidence, review ownership for context surfaces, and pinned stripped-install fallback inventories.
**Knowledge:** Run git fetch --unshallow before HISTORY SHA checks in shallow clones. AGENT_BUS legacy evidence is exact line plus content hash only; broad timestamp-era carve-outs are rejected. write_install_json fallback mirrors install-owned surfaces but not governed project-authored knowledge directories.

